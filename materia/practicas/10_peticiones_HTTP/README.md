# Solicitudes HTTP

En esta práctica realizaremos la misma aplicación 3 veces, pero en cada una de ellas usaremos una forma diferente de realizar solicitudes HTTP.

## Aplicación

Vamos a utilizar la API de [PokeAPI](https://pokeapi.co/) para obtener la imagen de un pokemon segun el nombre que ingrese el usuario.

1. Crea un formulario con un campo de texto y un botón de submit, donde el usuario pueda ingresar el nombre de un pokemon, encapsula todo en una etiqueta `form`.
2. 